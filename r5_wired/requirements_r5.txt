# R5 IRON OMEGA: Requirements
# ============================

# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.9

# R5.1: Neuro-Symbolic
# Fine-tuning (optional, GPU required):
#   axolotl>=0.4.0        # Install separately: pip install axolotl
#   torch>=2.2.0          # GPU build depends on CUDA version
#   transformers>=4.38.0
#   datasets>=2.17.0
#   peft>=0.9.0           # LoRA/QLoRA adapters
#   bitsandbytes>=0.42.0  # 4-bit quantization

# R5.2: Graph Ontology
neo4j>=5.17.0             # Official async driver

# R5.3: WebSockets (built into FastAPI/uvicorn)
websockets>=12.0

# R5.4: Distributed Cryptography (stdlib only for core; optional extensions)
# cryptography>=42.0.0   # For hardware key ceremony integration
# ecdsa>=0.18.0          # For ECDSA oath signatures

# Development / Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0             # For async API testing

# Utilities
networkx>=3.2.0           # CP-Net graph traversal (already in iron_omega)
